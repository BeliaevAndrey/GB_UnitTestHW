### Отчет

### Описание сценариев тестирования

```java
class MeanValuesCompareTest {
    
    @BeforeEach
    void setUp() {
        // Подготовка объектов тестируемого класса.
    }

    @Test
    void testCompareIntegersFirstGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков целых чисел;
        // Среднее значение первого списка больше.
        
        // Проверка вывода сообщения:
        String expected = "Первый список имеет большее среднее значение";
    }

    @Test
    void testCompareIntegersSecondGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков целых чисел;
        // Среднее значение второго списка больше.
        
        // Проверка вывода сообщения:
        String expected = "Второй список имеет большее среднее значение";
    }

    @Test
    void testCompareIntegersMeanValuesEquals() {
        // Успешный сценарий сравнения средних значений списков целых чисел;
        // Среднее значения списков равны.
        
        // Проверка вывода сообщения:
        String expected = "Средние значения равны";
    }

    @Test
    void testCompareLongsFirstGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков целых Long чисел;
        // Среднее значение первого списка больше.

        // Проверка вывода сообщения:
        String expected = "Первый список имеет большее среднее значение";
    }

    @Test
    void testCompareLongsSecondGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков целых Long чисел;
        // Среднее значение второго списка больше.

        // Проверка вывода сообщения:
        String expected = "Второй список имеет большее среднее значение";
    }

    @Test
    void testCompareLongsMeanValuesEquals() {
        // Успешный сценарий сравнения средних значений списков целых чисел;
        // Среднее значения списков равны.

        // Проверка вывода сообщения:
        String expected = "Средние значения равны";
    }


    @Test
    void testCompareDoublesFirstGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Среднее значение первого списка больше.

        // Проверка вывода сообщения:
        String expected = "Первый список имеет большее среднее значение";
    }

    @Test
    void testDoublesCompareSecondGreaterSuccess() {
        // Успешный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Среднее значение второго списка больше.

        // Проверка вывода сообщения:
        String expected = "Второй список имеет большее среднее значение";
    }

    @Test
    void testDoublesCompareMeanValuesEquals() {
        // Успешный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Среднее значения списков равны.

        // Проверка вывода сообщения:
        String expected = "Средние значения равны";
    }

    @Test
    void testCompareNullPointerExceptionFail() {
        // Негативный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Первый список null;

        // Проверка выбрасываемого исключения -- NullPointerException.
    }

    @Test
    void testCompareEmptyListFail() {
        // Негативный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Первый список пуст;
        
        // Проверка выбрасываемого исключения -- IllegalArgumentException
        // Проверка сообщения выбрасываемого исключения:
        String expected_message = "Передан пустой список";
    }


    @Test
    void testCompareTypeErrorFail() {
        // Негативный сценарий сравнения средних значений списков чисел с плавающей точкой Double;
        // Передаются списки строк;

        // Проверка выбрасываемого исключения -- IllegalArgumentException
        // Проверка сообщения выбрасываемого исключения:
        String expected_message = "Нечисловой тип значений";
    }

}
```